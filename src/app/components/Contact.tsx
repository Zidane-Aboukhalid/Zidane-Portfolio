// This component is no longer used as a standalone section.
// The contact content is now rendered directly in the contact page (src/app/contact/page.tsx).
export default function Contact() {
  return null;
}
