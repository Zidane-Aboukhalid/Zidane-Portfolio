// This component is no longer used as a standalone section.
// The hero content is now rendered directly in the home page (src/app/page.tsx).
// Keeping this file for backward compatibility.
export default function Hero() {
  return null;
}
