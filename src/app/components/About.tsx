// This component is no longer used as a standalone section.
// The about content is now rendered directly in the about page (src/app/about/page.tsx).
export default function About() {
  return null;
}
